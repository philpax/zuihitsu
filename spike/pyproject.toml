[project]
name = "zuihitsu-spike"
version = "0.0.0"
description = "Stage 0 model-floor spike (throwaway). Discarded before Stage 1."
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40",
]

[tool.uv]
package = false
