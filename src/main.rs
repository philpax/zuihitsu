//! The agent-server binary. The server itself lands in Stage 3; for now this is a placeholder so
//! the crate has a runnable entry point alongside the library.

fn main() {
    println!(
        "zuihitsu {} — the agent server is not yet implemented (see docs/spec.md, build order).",
        env!("CARGO_PKG_VERSION")
    );
}
