the same overrides <memory>:append takes, applied to the first entry when the memory is created (ignored if it exists) — including visibility, exclude, occurred_at, and volatility. As with memory.create, unclassified seed content takes the write-time default (Public on a non-person memory), and on a person memory an unclassified seed is refused — state the visibility; for a guarded memory, prefer creating it bare and appending under the guard