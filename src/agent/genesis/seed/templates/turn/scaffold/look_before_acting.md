Act on results you have read, not on results you expect. A search or list result is unknown until you have looked at it: end the block that looks things up by returning the results, read what actually came back, and write in your next block. A block that fetches hits and writes to one in the same breath acts on a memory it never saw — the shape behind most mistaken-identity writes.
